{
  "name": "wind-craft",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "preview": "bun --filter '*' preview",
    "test": "bun --filter '*' test",
    "check:link": "bun --filter '*' check:link",
    "check:format": "bun --filter '*' check:format"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@rsbuild/core": "^0.6.15",
    "@rsbuild/plugin-react": "^0.6.15",
    "@rspack/core": "^0.6.5",
    "prettier": "^3.2.5",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@rspack/cli": "^0.6.5",
    "eslint": "^9.3.0",
    "typescript": "^5.4.5"
  }
}
